const totalWinTon = (state) => state.profile.totalWinTon
const totalPurchasedTicket = (state) => state.profile.totalPurchasedTicket

const eventsHistory = (state) => state.profile.eventsHistory

export default {
  totalWinTon,
  totalPurchasedTicket,

  eventsHistory,
}